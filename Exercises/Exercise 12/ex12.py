age = input("How old are you? ")
height = input("How tall are you? ")
weight = input("How much do you weigh? ")

print(f"So, you're {age} years old, {height} tall and {weight} heavy.")


# use pydoc in terminal to search commands
# input() function: input(prompt) prompt is string (could be empty).
# "The prompt string, if given, is printed to standard output without a
# trailing newline before reading input." -pydoc

# "open(file, mode='r', buffering=-1, encoding=None, errors=None, newline=None,
# closefd=True, opener=None)
# Open file and return a stream.  Raise OSError upon failure." -pydoc

# file is a class
# "file(name[, mode[, buffering]]) -> file object, The file will be created if
# it doesn't exist when opened for writing or appending

# os is a Module
# "os - OS routines for NT or Posix depending on what system we're on." -pydoc

# sys is a buit-in Module
# Reference=https://docs.python.org/3,7/library/sys

# pydoc is also a Module
# "pydoc - Generate Python documentation in HTML or text for interactive
# use." -pydoc
